#pragma once

#include <string>

struct Config
{
    static inline const int WINDOW_WIDTH = 1280;
    static inline const int WINDOW_HEIGHT = 720;
    static inline const std::string WINDOW_TITLE = "KARTGAME";

    static inline const float DELTA_DIVIDEND = 1000.0f;
};
